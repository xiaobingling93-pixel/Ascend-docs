# 简介

_资料书写要求：_

-   _简要概述清楚XX模型是什么，有什么特点，以及性能优势。_
-   _如果有多个参数版本的模型，一并描述清楚，以及和老版本相比的能力提升。_
-   _有多项描述时，采用itemlist形式呈现。_

示例如下：

-  [Llama3（Large Language Model Meta AI 3）](https://gitee.com/link?target=https%3A%2F%2Fgithub.com%2Fmeta-llama%2Fllama3)和[Llama3.1](https://gitee.com/link?target=https%3A%2F%2Fgithub.com%2Fmeta-llama%2Fllama-models%2Ftree%2Fmain%2Fmodels%2Fllama3_1)，是由 Meta AI 发布的一个开放且高效的大型基础语言模型，可以通过自然语言交互的方式提供知识、文本生成、语言翻译、语言理解、代码编写和解释等任务。
-  Llama3当前包含两个参数版本：Llama3-8B和Llama3-70B。相较于Llama2，Llama3支持8K长文本，改进的tokenizer具有128K token的词汇量，可实现更好的性能；同时，Llama3在代码生成等任务上实现了领先，能够进行复杂的推理，更遵循指令并解决很多微妙的问题。Llama3.1包含3个参数版本：Llama3.1-8B、Llama3.1-70B-Instruct和Llama3.1-405B-Instruct，并进一步支持了RoPE-Scaling计算。
-  此代码仓中实现了一套基于NPU硬件的Llama3与Llama3.1推理模型。配合加速库使用，旨在NPU上获得极致的推理性能。

# 特性矩阵

_介绍该模型支持的特性，包括支持多卡并行能力、数据类型、量化方式、服务化类型，长序列特性等。_

_资料写作要求：_

_采用表格的形式，呈现模型和特性的支持情况_

示例如下：

|模型及参数量|800I A2 Tensor Parallelism|300I DUO Tensor Parallelism|FP16|BF16|Flash Attention|Paged Attention|W8A8量化|W8A16量化|KV cache量化|稀疏量化|Attention 量化|lccl 量化|MindIE Service|长序列|Multi LoRA|
|--|--|--|--|--|--|--|--|--|--|--|--|--|--|--|--|
|Llama3-8B|支持world size 1,2,4,8|支持world size 1,2,4,8|√|√|√|√|×|×|×|×|×|×|√|×|×|
|Llama3-70B|支持world size 8|×|√|√|√|√|×|√|×|×|×|×|√|×|×|
|Llama3.1-8B|支持world size 1,2,4,8|支持world size 1,2,4,8|√|√|√|√|√|×|×|×|×|√|√|×|×|
|Llama3.1-70B|支持world size 8|支持world size 2,4|√|√|√|√|×|×|√|√|√|×|√|√|√|
|Llama3.1-405B|支持world size 16(单卡64G)|×|√|√|√|√|×|×|×|×|×|×|×|×|×|


# 环境准备

资料书写要求：

说明部署所需的硬件资源、软件环境和模型权重文件。

## 硬件要求

_资料写作要求：_

_一句话描述清楚，部署该模型进行推理时需要满足的硬件依赖。_

示例：

部署DeepSeek-R1-Distill-Qwen-1.5B模型至少需要1台Atlas 800I A2服务器或1台Atlas 300I DUO服务器

用BF16权重进行推理至少需要4台Atlas 800I A2（8\*64G）服务器，用W8A8量化权重进行推理则至少需要2台Atlas 800I A2 \(8\*64G\)。

## 模型权重

### 下载模型权重

_资料写作要求：_

-   _下载权重前，必须明确要__预留的存储空间大小_，_并给出如何查询存储空间的命令和方法_
-   _（可选）如果多机推理，确保每台机器上有相同的权重，并根据权重大小选择对应的卡数推理_
-   _给出该模型下所有参数版本模型权重的下载路径（（HuggingFace，ModelScope，Modelers）），多个用itemlist格式呈现_，建议给_ModelScope_链接。
-   _（可选）如果有脚本批量下载的方式，也一并写明，给出脚本方式的具体步骤。包括但不限于在什么路径下修改什么配置，执行什么脚本文件等，并给出脚本文件中的参数说明，和配置建议。__参数说明采用表格形式，表头为参数名和含义。_
-   _命令必须采用screen格式_

如：权重下载

-   [Llama3-8B](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FMeta-Llama-3-8B%2Ftree%2Fmain)
-   [Llama3-70B](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FMeta-Llama-3-70B%2Ftree%2Fmain)
-   [Llama3.1-8B](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FLlama-3.1-8B%2Ftree%2Fmain)
-   [Llama3.1-70B](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FLlama-3.1-70B%2Ftree%2Fmain)
-   [Llama3.1-70B-Instruct](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FLlama-3.1-70B-Instruct%2Ftree%2Fmain)
-   [Llama3.1-405B-Instruct](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FLlama-3.1-405B-Instruct%2Ftree%2Fmain)
-   [Llama3.2-1B-Instruct](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FLlama-3.2-1B-Instruct%2Ftree%2Fmain)
-   [Llama3.2-3B-Instruct](https://gitee.com/link?target=https%3A%2F%2Fhuggingface.co%2Fmeta-llama%2FLlama-3.2-3B-Instruct%2Ftree%2Fmain)

### （可选）权重转换

_支持权重转换的模型才需要写，目前看是DeepSeek特有。_

_如果下载的权重不满足昇腾硬件推理，则需要进行转换，描述清楚进行权重转换要用的脚本文件、命令，并给出示例命令。_

1.  如果下载的模型权重是FP8的格式，则需要进行权重转换为BF16，昇腾硬件推理部署必须为BF16格式
2.  昇腾只支持加载safetensor格式的权重文件，若下载的是bin格式的权重文件，则需要按照XX方式进行转换

### （可选）权重量化

支持量化的模型才需要写，特性矩阵中支持哪些量化方式，这里就写几种量化权重的生成。

-   _介绍量化权重依赖的工具及使用，可参见msit工具的readme。_
-   _支持几种量化方式，写几种_。
-   _如何生成量化权重，并给出命令、相关参数的解释、相关限制_。

## 软件环境

_描述推理前，需要进行的软件环境准备，如物理机场景需要配套的相关软硬件，容器场景需要的镜像版本和下载链接等。有多个组件的配套建议采用表格形式呈现。_

> [!NOTE]说明
> 获取对应的安装包，不能描述根据芯片型号，只能描写根据产品型号，文档中不能出现芯片型号这样的描述。

1.  _执行推理前需要进行的准备工作，如：修改配置文件、文件夹属组和权限等。__有多项需要准备的，采用itemlist方式呈现。_
    -   _修改配置文件torch\_dtype字段__（根据需要执行的推理模式来修改模型配置文件$\{weight\_path\}/config.json中的torch\_dtype字段。）_

        如果需要执行FP16推理，那么将torch\_dtype字段修改为float16；执行BF16推理，则将torch\_dtype字段修改为bfloat16_。_

    -   _修改相关权限，如模型文件夹属组和执行权限等_
    -   _修改权重文件的congfig.json。_

2.  _（可选，如DeepSeek）如果是多机场景，还需要给出机器网络检查；获取每张卡的ip地址；创建__rank\_table\_file.json文件，并示例；给出配置文件中的相关参数说明_

# 安装

具体安装，可链接跳转至安装指南。

包括如下两种安装方式：

1.  _镜像安装_
2.  _源码安装_


*资料写作要求*：提供安装的具体步骤和命令_。

-   _涉及的命令要使用screen格式，参数需要给出解释，包括含义、取值范围、单位等_
-   _（可选）需要开启的基础环境变量和通信环境变量_，参数要给出解释，包括含义、取值范围、单位



# 纯模型推理

_资料写作要求：_

-   内容包括对话测试、精度测试和性能测试。
-   描述清楚进行纯模型对话测试、精度测试和性能测试的具体步骤，给出完整的命令示例和参数说明等。参数说明建议采用表格形式。
-   文中的示例代码或命令中的路径变量需要给出解释。
-   步骤用项目编号，命令用screen标记，注意层级。

## 对话测试

示例：

1.  执行如下命令，进入atb models路径。

    ```
    cd /usr/local/Ascend/atb-models
    ```

2.  执行如下命令，进行对话测试。

    ```
    export MINDIE_LOG_TO_STDOUT=1 
    bash examples/models/qwen/run_pa.sh -m ${weight_path} --trust_remote_code true
    ```

3.  回显如下XXX。

## 精度测试

示例如下：

1.  执行如下命令，进入modeltest路径。

    ```
    cd /usr/local/Ascend/atb-models/tests/modeltest/
    ```

1.  执行如下命令，运行测试脚本。

    ```
    bash run.sh pa_[data_type] [dataset] ([shots]) [batch_size] [model_name] ([is_chat_model]) [weight_dir] [rank_table_file] [world_size] [node_num] [rank_id_start] [master_address] ([parallel_params]) 
    ```

参数说明：

1.  data\_type：为数据类型，根据权重目录下config.json的data\_type选择bf16或者fp16，例如：pa\_bf16。
2.  dataset：可选full\_BoolQ、full\_CEval等，相关数据集可至[魔乐社区MindIE](https://gitee.com/link?target=https%3A%2F%2Fmodelers.cn%2FMindIE)下载，（下载之前，需要申请加入组织，下载之后拷贝到/usr/local/Ascend/atb-models/tests/modeltest/路径下）CEval与MMLU等数据集需要设置shots（通常设为5）。
3.  batch\_size：为batch数。
4.  model\_name：为deepseekv2。
5.  is\_chat\_model：为是否支持对话模式，若传入此参数，则进入对话模式。
6.  weight\_dir：为模型权重路径。
7.  rank\_table\_file：为“前置准备”中配置的rank\_table\_file.json路径。
8.  world\_size：为总卡数。
9.  node\_num：为当前节点编号，即rank\_table\_file.json的server\_list中顺序确定。
10. rank\_id\_start：为当前节点起始卡号，即rank\_table\_file.json中当前节点第一张卡的rank\_id，Atlas 800I-A2双机场景下，主节点为0，副节点为8。
11. master\_address：为主节点ip地址，即rank\_table\_file.json的server\_list中第一个节点的ip。
12. parallel\_params: 接受一组输入，格式为\[dp,tp,moe\_tp,moe\_ep,pp,microbatch\_size\],如\[8,1,8,-1,-1,-1\]

## 性能测试

略，要求同精度测试

# 服务化推理

（可选）如果模型支持对接服务化，则需要给出服务化推理的描述。

_资料写作要求：_

-   内容包括配置环境变量、修改服务化参数、拉起服务，发送请求和精度测试，性能测试。
-   描述清楚每个操作的具体步骤，给出完整的命令示例和参数说明等。_参数说明建议采用表格形式。
-   需要用户修改的参数，需要罗列全面，并给出建议参数值配置，以及参数说明。
-   执行命令，有回显的，需要给出__成功的回显信息_。
-   文中的示例代码或命令中的路径变量需要给出解释。
-   步骤用项目编号，命令用screen标记，注意层级。

## 配置服务化环境变量

_给出环境变量的含义和示例的命令_。

示例如下：

expandable\_segments-使能内存池扩展段功能，即虚拟内存特性。

```
export PYTORCH_NPU_ALLOC_CONF=expandable_segments:True
```

## 修改服务化参数

_描述配置文件中需要修改的参数以及参数说明_。

```
cd /usr/local/Ascend/mindie/latest/mindie-service/ 
vim conf/config.json 
```

修改以下参数：

```
"httpsEnabled" : false, # 如果网络环境不安全，不开启HTTPS通信，即“httpsEnabled”=“false”时，会存在较高的网络安全风险 
... 
"multiNodesInferEnabled" : true, # 开启多机推理 
... 
# 若不需要安全认证，则将以下两个参数设为false 
"interCommTLSEnabled" : false, 
"interNodeTLSEnabled" : false, 
... 
"npudeviceIds" : [[0,1,2,3,4,5,6,7]], 
... 
"modelName" : "DeepSeek-R1" # 不影响服务化拉起 
"modelWeightPath" : "权重路径",
 "worldSize":8,
```

详细的参数说明请参见XXX。

## 拉起服务

```
 以下命令需在所有机器上同时执行 
# 解决权重加载过慢问题 
export OMP_NUM_THREADS=1 
# 设置显存比 
export NPU_MEMORY_FRACTION=0.95 
# 拉起服务化 
cd /usr/local/Ascend/mindie/latest/mindie-service/ ./bin/mindieservice_daemon
```

执行命令后，首先会打印本次启动所用的所有参数，然后直到出现以下输出：

```
Daemon start success!
```

## 发送请求

示例如下：

重开一个窗口，使用以下命令发送请求。例如验证服务是否拉起：

```
curl -H "Accept: application/json" -H "Content-type: application/json" -X POST -d '{
"prompt": "My name is Olivier and I ",
"max_tokens":10
}' http://127.0.0.1:1025/generate
```

回显如下，表明请求发送成功：

```
{"text":["My name is Olivier and I  25 years old. I am a French student"]}
```

## 精度测试

_介绍模型性能测试的工具和使用样例。_

_资料写作要求：_

_写清楚进行精度测试的工具，包括工具的安装，以及数据集的准备，获取路径，和放置路径等。_

_给出精度测试的配置样例和执行命令。_

_执行精度测试命令后，需要给出回显信息，并写清楚需关注的回显参数，参数含义。_

_（可选）开启必须的环境变量，确定性计算等。并给出环境变量设置的说明。_

1.  参见附件，获取XX数据集，并放置于AISBench工具根路径的ais\_bench/datasets文件夹下。
2.  配置ais\_bench/benchmark/configs/models/vllm\_api/vllm\_api\_stream\_chat.py文件，示例如下所示。

    ```
    from ais_bench.benchmark.models import VLLMCustomAPIChatStream  
    models = [     
        dict(         
            attr="service",         
            type=VLLMCustomAPIChatStream,         
            abbr='vllm-api-stream-chat',         
            path="/home/weight",                    # 指定模型序列化词表文件绝对路径，一般来说就是模型权重文件夹路径        
            model="qwen2-7b",        # 指定服务端已加载模型名称，依据实际VLLM推理服务拉取的模型名称配置（配置成空字符串会自动获取）        
            request_rate = 0,           # 请求发送频率，每1/request_rate秒发送1个请求给服务端，小于0.1则一次性发送所有请求        
            retry = 2,         
            host_ip = "127.0.0.1",      # 指定推理服务的IP        
            host_port = 1025,           # 指定推理服务的端口        
            max_out_len = 512,          # 推理服务输出的token的最大数量        
            batch_size=1,               # 请求发送的最大并发数
            trust_remote_code=False,         
            generation_kwargs = dict(             
                temperature = 0.5,             
                top_k = 10,             
                top_p = 0.95,             
                seed = None,             
                repetition_penalty = 1.03,                                
            ) , 
             pred_postprocessor=dict(type=extract_non_reasoning_content)   
        ) 
    ]
    ```

3.  执行以下命令启动服务化精度测试。

    ```
    ais_bench --models vllm_api_stream_chat --datasets demo_gsm8k_gen_4_shot_cot_chat_prompt --debug
    ```

    回显如下所示则表示执行成功：

    ```
    dataset                 version  metric   mode  vllm_api_general_chat 
    ----------------------- -------- -------- ----- ---------------------- 
    demo_gsm8k              401e4c   accuracy gen                   62.50
    ```

## 性能测试

_介绍模型性能测试的工具和使用样例。_

_资料写作要求：_

_写清楚进行性能测试的工具，以及进行性能测试的数据集获取，放置路径等。_

_给出性能测试的配置样例和执行命令。_

_执行性能测试命令后，需要给出回显信息，并写清楚需关注的回显参数，参数含义。_

_（可选）开启必须的环境变量，确定性计算等。并给出环境变量设置的说明。_

回显如下所示则表示执行成功：

```
XXXXX
```

# 性能调优

介绍性能调优手段。

热门模型必选：如DeepSeek V3，Qwen3.....

# 附录


## 数据集下载

_资料写作要求：_

_给出支持的数据集以及下载地址，可用表格形式也可用itemlist形式。_

# FAQ

**后续可以直接给链接到docs下的FAQ**

_提供推理过程中遇到的__一些__常见问题处理方法，包括问题描述、可能原因和解决措施。_

_针对可能原因给出的解决措施，应该具备可操作性，能step by step能帮忙用户解决该FAQ。_


**问题描述**


**可能原因**


**解决措施**


# 声明

_统一的安全声明_

-   本代码仓提到的数据集和模型仅作为示例，这些数据集和模型仅供您用于非商业目的，如您使用这些数据集来完成示例，请您特别注意应遵守对应数据集合模型的License，如您因使用数据集或者模型而产生侵权纠纷，华为不承担任何责任。
-   如您在使用本地代码的过程中，发现任何问题（包括但不限于功能问题、合规问题），请在本代码仓提交issue，我们将及时审视并解答。

