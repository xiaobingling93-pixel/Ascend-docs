# 安全声明

*安全声明并不是所有工具都有的，根据工具自身情况选择是否需要增加安全声明。包括下边的所有section的内容，可根据工具自身的情况选择，如果涉及，请选择，如果不涉及，可忽略对应section的内容。*

*生态资料涉及安全声明的产品包括MindSpeed、torchair、ModelZoo-PyTorch、MindSpeed-LLM、Ascend Extension for PyTorch、CANN融合算子库、CANN集合通信库插件，请其余产品审视，如果有，需要增加安全声明，如果没有，则可忽略。*

## 系统安全加固

*系统需要安全加固的方法。*

## 运行用户建议

*对运行用户有要求的可给出合理建议。*

示例：

出于安全性及权限最小化角度考虑，不建议使用root等管理员类型账户使用TorchAir。

## 文件权限控制

*文件权限管理建议。例如，工具安装后的目录，如果多用户使用的话，设置文件权限管控更安全。如果每个目录所需的最大权限不一致的话，建议以表格形式呈现对应目录所需的最大权限。*

示例：

建议用户在主机（包括宿主机）及容器中设置运行系统umask值为0027及以上，保障新增文件夹默认最高权限为750，新增文件默认最高权限为640。

## 数据安全

*在工具使用过程中，会生成日志及数据，需提醒用户注意数据安全。*

示例：

MindSpeed依赖CANN的基础能力实现AOE性能调优、算子dump、日志记录等功能，用户需要关注上述功能生成文件的权限控制。

## 调试工具（可选）

*如果当前产品需要集成其它调试工具，可在此section写明集成原因，使用场景，机器使用的风险提示。*

示例：

TorchAir内集成生成数据dump调试工具：

- 集成原因：提供NPU图模式中间算子数据dump功能，用于比对精度，加速图模式开发调试过程。
- 使用场景：默认不开启，如用户使用pytorch图模式且需要进行精度分析时，可在模型启动脚本中调用数据dump接口生成dump数据。
- 风险提示：使用该功能会在本地生成计算数据，用户需加强对相关dump数据的保护，请在图模式调试阶段使用该能力，调试完毕后及时关闭。

## 运行安全

*产品在运行过程中出现的一些异常情况，提醒用户注意。*

示例：

MindSpeed 在运行异常时会退出进程并打印报错信息，建议根据报错提示定位具体错误原因，包括设定算子同步执行、查看CANN日志、解析生成的Core Dump文件等方式。

## 公网地址

*出于安全要求，公网地址需公开，如果公网地址比较少，可在此section使用表格记录，如果公网地址较多，建议使用excel表记录，使用链接形式链接到公网地址excel表中，或以附件形式上传。*

## 公开接口（可选）

*如果涉及公开接口，说明是哪些接口，用户如何使用。*

示例：

MindSpeed已更新其接口策略，现在除了对原生megatron在昇腾设备的无缝支持，还新增了针对融合算子的公开接口。用户在使用时，可以直接调用这些新增的融合算子接口，以充分利用MindSpeed在特定计算任务上的优化能力。

## 通信安全加固（可选）

*如果存在设备间的通信，且为了降低风险，建议用户加固，给出加固方法。*

## 通信矩阵

*出于安全要求，通信矩阵需公开，如果通信矩阵内容比较少，可在此section使用表格记录，如果通信矩阵内容较多，建议使用excel表记录，使用链接形式链接到通信矩阵excel表中，或以附件形式上传。*
